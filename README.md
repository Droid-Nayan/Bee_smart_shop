SmartShop (also known as Bee Shopping Mart) is a simple online shopping website where users can browse products, add them to the cart, apply discount coupons, and place orders.
It is built using HTML, Tailwind CSS, and JavaScript (DOM). Product data is fetched from the FakeStore API to make the site dynamic and realistic.
